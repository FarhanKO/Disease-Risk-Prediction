# Disease-Risk-Prediction

A combination of projects. Basically heart, lung, kidney, brain and other organ's wellbeing

Missing lung models: your lung X-ray dataset is at E:\Lung_Xray (6 classes: Covid-19, Emphysema, Normal, Pneumonia-Bacterial, Pneumonia-Viral, Tuberculosis). But its checkpoints/ folder is empty, so there are no trained lung models on this machine. We'll need to download them from the Drive link, if it's public, or retrain them.

Folder names don't match: tabular vs tabuler, and image_based vs image-based. It's worth fixing this once, early, before we write code that imports across modules.
Placeholder files: the nn files only exist so Git keeps the empty folders.
